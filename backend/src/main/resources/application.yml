spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: clickcart-backend
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/clickcart}

server:
  port: ${SERVER_PORT:8080}

clickcart:
  seed-data: ${CLICKCART_SEED_DATA:true}

